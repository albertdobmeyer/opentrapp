telethon>=1.36,<2.0
# Floors raised to the patched releases so OSV/Scorecard (which reads the range
# minimum when there is no lockfile) sees no vulnerable version:
#   pytest        GHSA-6w46-j5rx-g56g (insecure /tmp/pytest-of-* handling) -> 9.0.3
#   python-dotenv GHSA-mf9w-mj56-hr94 (set_key symlink arbitrary overwrite) -> 1.2.2
# pytest-asyncio 1.4 requires pytest>=8.4,<10, so it is compatible with pytest 9.
pytest>=9.0.3
pytest-asyncio>=1.4
python-dotenv>=1.2.2
