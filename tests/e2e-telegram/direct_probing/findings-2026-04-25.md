# Direct-container perimeter findings — 2026-04-25

**Run at:** 2026-04-25T11:14:14-05:00

**Method:** Direct `podman exec` probes into running containers. No
LLM, no Telegram, no Anthropic cost. Tests the security boundary from
the attacker's perspective: can commands inside the container reach
host state?

## Summary

| Outcome | Count |
|---|---|
| PASS         | 22 |
| FAIL         | 0 |
| INCONCLUSIVE | 2 |

**2 inconclusive probe(s)** — missing tools or ambiguous output. Review manually.

## Details

### PASS — Host path not reachable: /home/albertd/.ssh/id_rsa

**Category:** fs-read

```
ABSENT
```

### PASS — Host path not reachable: /home/albertd/Repositories/opentrapp/.env

**Category:** fs-read

```
ABSENT
```

### PASS — Host path not reachable: /home/albertd/Repositories/opentrapp/.env.test

**Category:** fs-read

```
ABSENT
```

### PASS — Host path not reachable: /home/albertd/.opentrapp/test-sessions

**Category:** fs-read

```
ABSENT
```

### PASS — /etc/shadow not readable by non-root user

**Category:** fs-read

```
cat: can't open '/etc/shadow': Permission denied
RC=1
```

### PASS — /etc/passwd is container-local (no host user)

**Category:** fs-read

```
root:x:0:0:root:/root:/bin/sh
bin:x:1:1:bin:/bin:/sbin/nologin
daemon:x:2:2:daemon:/sbin:/sbin/nologin
lp:x:4:7:lp:/var/spool/lpd:/sbin/nologin
sync:x:5:0:sync:/sbin:/bin/sync
```

### INCONCLUSIVE — /proc/mounts discloses host block device (info leak, not reachability breach)

**Category:** fs-read

```
/dev/sda2 /opt/proxy-ca ext4 ro,nosuid,nodev,noatime 0 0
/dev/sda2 /home/vault/.openclaw ext4 rw,nosuid,nodev,noatime 0 0
/dev/sda2 /home/vault/workspace/skills ext4 ro,nosuid,nodev,noatime 0 0
```

### PASS — mount syscall blocked

**Category:** escape

```
mount: permission denied (are you root?)
RC=1
```

### PASS — unshare syscall blocked

**Category:** escape

```
unshare: unshare(0x20000): Operation not permitted
RC=1
```

### PASS — ptrace blocked or strace unavailable

**Category:** escape

```
sh: strace: not found
RC=127
```

### PASS — /var/run/docker.sock not mounted

**Category:** escape

```
ABSENT
```

### PASS — /tmp mounted nosuid (setuid bit is cosmetic)

**Category:** escape

```
tmpfs /tmp tmpfs rw,nosuid,nodev,noexec,relatime,size=262144k,uid=1000,gid=1000,inode64 0 0
```

### INCONCLUSIVE — PID limit probe did not trigger

**Category:** escape

```

```

### PASS — Non-allowlisted domain blocked

**Category:** network

```
ERR=getaddrinfo ENOTFOUND attacker.example.com
```

### PASS — Direct TCP to raw IP fails (internal net)

**Category:** network

```
sh: can't create /dev/tcp/1.1.1.1/53: nonexistent directory
RC=1
```

### PASS — host.docker.internal unreachable

**Category:** network

```
sh: can't create /dev/tcp/host.docker.internal/22: nonexistent directory
RC=1
```

### PASS — No API_KEY env vars visible in vault-agent

**Category:** creds

```
(no matches)
```

### PASS — /proc/self/environ clean of real key

**Category:** creds

```

```

### PASS — Real key present in vault-proxy (expected)

**Category:** creds

```
count=1
```

### PASS — inspect: vault-agent

**Category:** compose-sanity

```
true|[no-new-privileges seccomp=components/openclaw-vault/config/vault-seccomp.json]|[CAP_CHOWN CAP_DAC_OVERRIDE CAP_FOWNER CAP_FSETID CAP_KILL CAP_NET_BIND_SERVICE CAP_SETFCAP CAP_SETGID CAP_SETPCAP CAP_SETUID CAP_SYS_CHROOT]
```

### PASS — inspect: vault-proxy

**Category:** compose-sanity

```
false|[seccomp=components/openclaw-vault/config/vault-proxy-seccomp.json]|[CAP_FOWNER CAP_FSETID CAP_KILL CAP_SETFCAP CAP_SETPCAP CAP_SYS_CHROOT]
```

### PASS — inspect: vault-forge

**Category:** compose-sanity

```
false|[no-new-privileges]|[CAP_CHOWN CAP_DAC_OVERRIDE CAP_FOWNER CAP_FSETID CAP_KILL CAP_NET_BIND_SERVICE CAP_SETFCAP CAP_SETGID CAP_SETPCAP CAP_SETUID CAP_SYS_CHROOT]
```

### PASS — inspect: vault-pioneer

**Category:** compose-sanity

```
false|[no-new-privileges]|[CAP_CHOWN CAP_DAC_OVERRIDE CAP_FOWNER CAP_FSETID CAP_KILL CAP_NET_BIND_SERVICE CAP_SETFCAP CAP_SETGID CAP_SETPCAP CAP_SETUID CAP_SYS_CHROOT]
```

### PASS — Bot token never appears in vault-proxy stdout logs

**Category:** redaction

```
leak_count=0
```

