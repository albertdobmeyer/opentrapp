# Dependencies
node_modules/

# Phase B (ADR-0019): the daemon sidecar is built + staged here by CI per
# target triple (bundle.externalBin); never committed.
app/src-tauri/binaries/

# Local-only docs
docs/code-signing-guide.md

# Maintainer-only operational notes — kept out of the public repo because
# they include adversarial-test recipes (e.g. registry-staging procedures)
# that would weaken the security story if mirrored here.
HUMAN-TODO.md

# Build outputs
app/src-tauri/target/
app/src-tauri/gen/
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Environment
.env
.env.local

# Test-harness secrets and caches
.env.test
.env.test.local
*.session
*.session-journal
tests/e2e-telegram/.venv/
tests/e2e-telegram/.pytest_cache/
tests/e2e-telegram/artifacts/

# Python build artifacts (anywhere in the tree)
__pycache__/
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Claude Code local settings
.claude/

# Superpowers brainstorm sessions
.superpowers/

# Playwright MCP artifacts
.playwright-mcp/

# Test screenshots
test-screenshots/

# Logs
*.log

# Playwright test results
test-results/

# Generated perimeter bundle resources: policy files staged by build.rs
# from the submodules, and image tarballs + image-digests.json dropped by CI.
app/src-tauri/resources/perimeter/

# Agent working scratchpad — future-work list, not part of the repo
/AGENT-TODO.md
/MISSION.md

# Outreach drafts — kept out of public history until they're sent / accepted.
# The opencode pitch (Thread D step 4) drafts here.
/docs/pitch-opencode.md

# Runtime scan artifacts (social feed scanner / census output)
/data/

# Coverage reports (generated by `npm run test:coverage` / cargo-llvm-cov)
/app/coverage/
/app/src-tauri/target/llvm-cov/
/app/.nyc_output/
/app/coverage-e2e/
/app/coverage-merged/
