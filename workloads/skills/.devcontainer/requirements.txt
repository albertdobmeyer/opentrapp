# Hash-pinned Python deps for the clawhub-workbench devcontainer so the build is
# reproducible and OpenSSF Scorecard's pinned-dependencies check sees a pinned
# pipCommand (consumed by .devcontainer/setup.sh via `pip install --require-hashes`).
#
# The devcontainer installs Python 3.12 (devcontainer.json python feature) on a
# glibc/Debian base (javascript-node:20), so pip resolves the cp312 manylinux
# wheel: x86_64 covers Codespaces, aarch64 covers Apple-Silicon local devcontainers.
# Hashes verified with `pip download pyyaml==6.0.2 --only-binary :all: ... | sha256sum`.
pyyaml==6.0.2 \
    --hash=sha256:80bab7bfc629882493af4aa31a4cfa43a4c57c83813253626916b8c7ada83476 \
    --hash=sha256:1f71ea527786de97d1a0cc0eacd1defc0985dcf6b3f17bb77dcfc8c34bec4dc5
